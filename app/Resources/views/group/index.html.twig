{% extends 'base.html.twig' %}

{% block body %}
    <h1>Group list</h1>

    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Groupid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for group in groups %}
            <tr>
                <td><a href="{{ path('group_show', { 'id': group.groupId }) }}">{{ group.name }}</a></td>
                <td>{{ group.groupId }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('group_show', { 'id': group.groupId }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('group_edit', { 'id': group.groupId }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('group_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
